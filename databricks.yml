bundle:
  name: model-guru

sync:
  include:
    - .build

artifacts:
  default:
    build: apx build

resources:
  apps:
    model-guru-app:
      name: "model-guru"
      description: "model-guru created with apx"
      source_code_path: ./.build

targets:
  dev:
    mode: development
    default: true
